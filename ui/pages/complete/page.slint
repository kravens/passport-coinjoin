import { Theme, UISize } from "@ui/theme.slint";
import { CjButton } from "../../cjbutton.slint";
import { Card } from "@ui/card.slint";
import { Chip, ChipColor } from "@ui/chip.slint";
import { Navigate } from "../../gen/navigate.slint";
import { Cj } from "../../cj.slint";

export component CompletePage inherits Rectangle {
    background: Theme.palette-background;
    VerticalLayout {
        width: parent.width;
        height: parent.height;
        padding-left: 24px;
        padding-right: 24px;
        padding-top: 24px + UISize.control-center-status-bar-height;
        padding-bottom: 24px;
        spacing: 16px;
        Text {
            text: "Session complete";
            color: Theme.palette-foreground;
            font-family: "Montserrat";
            font-size: 24px;
            font-weight: 700;
            horizontal-alignment: center;
        }
        HorizontalLayout {
            alignment: center;
            Chip { text: Cj.rounds + " rounds signed"; color: ChipColor.success; }
        }
        Card {
            width: 100%;
            vertical-stretch: 0;
            size: lg;
            eyebrow: "coinjoin.nl";
            title: "Coinjoin finished";
            body: Cj.rounds + " rounds signed for this session. Every output returned to your own wallet, and total fees stayed within your authorized cap.";
            footer: "Session key cleared from memory.";
        }
        Rectangle { vertical-stretch: 1; }
        CjButton {
            width: 100%;
            text: "Done";
            clicked => { Navigate.main-page({ }); }
        }
    }
}
